# Copy to .env.local in this folder. Same values as Bruno dwt-sandbox / dev.
# Do not commit .env.local and do not paste these into chat.

API_BASE=
TOKEN_URL=
CLIENT_ID=
CLIENT_SECRET=
API_KEY=

# From DwtOnboarding output OnboardingApiBaseUrl (no trailing slash).
# Needed for software-provider and operator signup forms.
ONBOARDING_API_BASE=
