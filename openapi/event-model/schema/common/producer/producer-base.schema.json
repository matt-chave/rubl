{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "producer-base.schema.json",
  "title": "Producer base fields",
  "description": "Fields present on every Producer, regardless of wasteSource.",
  "type": "object",
  "properties": {
    "councilMovement": {
      "type": "boolean",
      "description": "Whether this movement is carried out by, or on behalf of, a council."
    }
  },
  "required": ["councilMovement"]
}
