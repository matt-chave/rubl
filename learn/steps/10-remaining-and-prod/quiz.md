# Step 10 quiz

1. What does `GET /fate-of-waste` read — the event log or a projection?
2. Why might `POST /deliveries` return *more than one* `deliveryId`?
3. Name one thing you change for prod vs this sandbox.
4. In which order do you `cdk destroy` the stacks, and why?
5. What is still out of scope for v1 (name one)?
