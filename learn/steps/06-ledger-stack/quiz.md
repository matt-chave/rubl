# Step 06 quiz

1. What is the sort key of a legal event vs the live snapshot?
2. Why snapshot to the history table on PUT?
3. How is a unique `movementId` minted under concurrent load?
4. Why enable DynamoDB Streams *before* the API exists?
5. What command must application code avoid on the event store?
