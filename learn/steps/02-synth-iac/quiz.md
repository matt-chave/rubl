# Step 02 quiz

1. What command turns CDK TypeScript into CloudFormation without creating resources?
2. Why is charging its own stack *and* behind SQS?
3. What is the dummy AWS account in `app.ts` for?
4. Why do we use API Gateway **REST** (v1), not HTTP API?
5. What problem does DynamoDB Streams → EventBridge Pipe avoid?
