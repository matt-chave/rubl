# Step 05 quiz

1. Which OAuth2 *grant* do vendor systems use, and why not the authorization code grant?
2. Why must Lambdas not call Cognito on every `POST /movements`?
3. What two credentials will the movements API require later (step 07)?
4. Where do you store the client secret after software-provider signup?
5. What would you replace Cognito with in a Defra production design?
6. Why is `POST /software-providers` a registration front door rather than part of the movements API?
7. Does the `dwt-software-provider-signup` widget call Cognito or hold the client secret?
