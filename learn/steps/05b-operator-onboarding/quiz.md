# Step 05b quiz

1. What does a waste operator receive at signup, and what do they *not* receive?
2. Why is the operator profile stored on `DwtOnboarding` rather than the movements ledger?
3. Can the new API key call `POST /movements` immediately after this lesson? Why or why not?
4. How do movements Lambdas resolve an operator from a non-sandbox API key?
5. Why must the `dwt-operator-signup` widget not keep the API key?
