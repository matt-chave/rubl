# Step 07 quiz

1. When is ingestion “done” — 201 from API Gateway, or the object in S3?
2. What HTTP status do you expect for a missing `apiCode`?
3. Why are there two credentials, not one?
4. Where is the speakable ID generated?
5. Why can this step succeed even if Firehose is not deployed yet?
