# Step 03 quiz

1. Name one field that is required on `POST /movements`.
2. Why is validation split by *data group* rather than only by endpoint?
3. What is the difference between `validation.ts` (now a folder) and `rules.ts`?
4. Why is the EventBridge `payload` a JSON string in the data lake?
5. If AJV compiled the OpenAPI spec, what would we still need `rules.ts` for?
