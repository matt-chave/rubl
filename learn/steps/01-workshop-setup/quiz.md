# Step 01 quiz

Write answers in a notebook (or a scratch file you will not commit). Then open `quiz-answers.md`.

1. Why do we run unit tests before any AWS deploy?
2. What does a year-prefixed sqid like `25HRA0B2` identify, and should a vendor parse the `25`?
3. Where should you *not* paste AWS access keys?
4. What file will capture questions you ask in Cursor?
5. What Node major version does this project require?
