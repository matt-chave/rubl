# Step 01 answers

1. Failures are cheap and local. You learn the domain contract on your laptop before you pay for, or debug, cloud resources.
2. It identifies a public Movement ID. The vendor must not parse the `25`: the OpenAPI spec says the format is opaque, and the year prefix is only a generation detail.
3. Do not paste them into Cursor chat, into git, or into screenshots you might share. Keep them in an AWS CLI profile or SSO session.
4. `learn/qa/index.md`
5. Node 22 or newer, as stated in `package.json` `engines`.
