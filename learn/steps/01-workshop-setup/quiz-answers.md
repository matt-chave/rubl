# Step 01 answers

1. Failures are cheap and local. You learn the domain contract before paying for (or debugging) cloud resources.
2. A public Movement ID. **No** — the OpenAPI spec says the format is opaque. The year prefix is a generation detail, not a public API.
3. Into Cursor chat, git, or screenshots you might share. Use AWS CLI profiles / SSO.
4. `learn/qa/index.md`
5. 22 or newer (`package.json` `engines`).
