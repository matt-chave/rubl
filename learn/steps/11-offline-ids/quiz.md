# Step 11 quiz

1. Why must an unused reserved ID **not** be handed to another operator after 30 days?
2. Does `POST /id-reservations` write a waste EVENT (and therefore charge / land in bronze)?
3. Where does uniqueness come from — randomness, or something else?
4. If you reserve a `movementId` and then `POST /movements` **without** that field, what happens?
5. A reserved `deliveryId` is ignored in which delivery case?
6. You already have 10 unused reserved IDs and ask for 50 more. How many new IDs do you get?
