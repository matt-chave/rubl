# Local BFF only (apps/dwt-bff). Copy to apps/dwt-bff/.env.local — never commit secrets.
# These are the same values as Bruno dwt-sandbox / dev. Do not paste them into chat.

API_BASE=
TOKEN_URL=
CLIENT_ID=
CLIENT_SECRET=
API_KEY=
